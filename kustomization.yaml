apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hermes

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml

# Secret no incluido por defecto — aplicar manualmente o via External Secrets
# - secret.yaml
